name: AdminTools
version: 1.4
main: com.thepastimers.AdminTools.AdminTools
softdepend: [Database,Permission,Logger,Coord,UserMap]
commands:
    ban:
        description: bans user
    kick:
        description: kicks user
    pardon:
        description: pardon user
    entities:
        description: entities command
    ping:
        description: get player ping
    spawner:
        description: gives spawner